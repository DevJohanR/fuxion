/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Elimina o comenta la línea 'appDir' si no es necesaria
    // appDir: true,
  },
}

module.exports = nextConfig;
